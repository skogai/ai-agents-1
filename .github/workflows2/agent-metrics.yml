# Agent Metrics Collection Workflow
#
# Automatically collects and reports agent usage metrics on a weekly basis.
# Implements automated measurement for the 8 key metrics defined in docs/agent-metrics.md
#
# Schedule: Weekly on Sundays at 00:00 UTC
# Manual trigger: workflow_dispatch
#
# GitHub Issue: #7

name: Agent Metrics

on:
  schedule:
    # Run weekly on Sundays at midnight UTC
    - cron: '0 0 * * 0'

  workflow_dispatch:
    inputs:
      days:
        description: 'Number of days to analyze'
        required: false
        default: '7'
        type: string
      output_format:
        description: 'Output format'
        required: false
        default: 'markdown'
        type: choice
        options:
          - summary
          - markdown
          - json

permissions:
  contents: read

jobs:
  collect-metrics:
    name: Collect Agent Metrics
    # ADR-025: ARM runner for cost optimization (37.5% savings vs x64)
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98
        with:
          # Fetch full history for accurate metrics
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@28f2168f4d98ee0445e3c6321f6e6616c83dd5ec
        with:
          python-version: '3.14'

      # ADR-006: source selection is a GitHub expression (configuration), and
      # validation lives in scripts/workflows/resolve_dispatch_input.py. Two
      # deliberate behaviour changes: the `||` fallback now supplies the
      # declared default when the input is blank (the old `case` failed the
      # step instead), and `days=0` is now rejected (the old digit glob let it
      # through even though its own error message said "positive integer").
      - name: Determine analysis period
        id: period
        env:
          DAYS_RAW: ${{ (github.event_name == 'workflow_dispatch' && inputs.days) || '7' }}
          FORMAT_RAW: ${{ (github.event_name == 'workflow_dispatch' && inputs.output_format) || 'markdown' }}
        run: |
          OUTPUT_NAME=days INPUT_VALUE="$DAYS_RAW" VALUE_KIND=integer \
            python scripts/workflows/resolve_dispatch_input.py
          OUTPUT_NAME=format INPUT_VALUE="$FORMAT_RAW" VALUE_KIND=choice \
            ALLOWED_CHOICES=summary,markdown,json \
            python scripts/workflows/resolve_dispatch_input.py

      - name: Collect metrics
        id: metrics
        env:
          PERIOD_DAYS: ${{ steps.period.outputs.days }}
          PERIOD_FORMAT: ${{ steps.period.outputs.format }}
        run: python scripts/ci/collect_metrics_and_report.py

      - name: Display metrics
        run: cat metrics-report.txt

      - name: Upload metrics artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: agent-metrics-${{ github.run_number }}
          path: metrics-report.txt
          retention-days: 7

  # Optional: Create issue if metrics are below targets
  check-thresholds:
    name: Check Metric Thresholds
    # ADR-025: ARM runner for cost optimization (37.5% savings vs x64)
    runs-on: ubuntu-24.04-arm
    needs: collect-metrics

    steps:
      - name: Checkout repository
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@28f2168f4d98ee0445e3c6321f6e6616c83dd5ec
        with:
          python-version: '3.14'

      - name: Check thresholds
        id: check
        run: python scripts/ci/check_metrics_thresholds.py

      - name: Summary
        env:
          CHECK_COVERAGE: ${{ steps.check.outputs.coverage }}
          CHECK_INFRA_RATE: ${{ steps.check.outputs.infra_rate }}
        run: python scripts/ci/write_metrics_threshold_summary.py
